# 03 : Backlog par phase et lane

Chaque lane vit dans `~/Dev/cortex--<lane>`, branche `lane/<lane>`, ne touche que les fichiers de sa colonne « Possède ». Ordre de merge : B, C, D, E, F, G. Les acceptations se prouvent par la commande et sa sortie, jamais par une déclaration.

| Phase | Contenu | Possède | Acceptation mesurable | État |
|---|---|---|---|---|
| **A1 Repo** | Dépôt `~/Dev/cortex` : `git subtree split` des huit dossiers depuis le dépôt de skills du fabricant, disposition `skills/` et `fabricant/`, manifestes plugin, LICENSE, README, chemins repointés (`fabrique.py`, `parcours_blanc.py`, `rend_deck.py`), plugin installé en local, dossiers retirés du dépôt d'origine avec fichier de redirection | tout | `claude plugin install cortex@cortex-kit` réussit depuis la marketplace locale ; `claude plugin details cortex@cortex-kit` liste 13 skills ; `python3 skills/cortex-4-installation/recette/parcours_blanc.py; echo $?` rend 0 (38 contrôles) | fait, `d386243` |
| **A2 Contrat** | Ce pack ; `04-contrat.md` ; `config.example.yaml` avec toutes les clés v2 ; stub `notice.py` ; section « Notice » en fin des sept SKILL.md ; `fixtures.py` | `chantiers/`, `template/config.example.yaml`, `scripts/notice.py`, `recette/fixtures.py`, la section Notice des SKILL.md | `cortex_config.py` charge l'exemple sans erreur ; `fixtures.py` génère trois arbres en moins de 10 s, déterministes ; sept SKILL.md portent la ligne notice ; recette v1 à 0 ; grep white-label vide sur `chantiers/` et `skills/cortex-*` | fait |
| **B Poste et notice** (après A1) | `cortex-0-poste` (SKILL.md avec la phrase d'entrée « installe mon second cerveau », `scripts/poste.py` : détection OS, outils manquants avec la commande, mail par question et MX, écriture de `_cortex/poste.json` et du bloc `poste`) ; `outils/OUTILS.md` ; `etat.py` à neuf étapes (`04-contrat.md` §5) ; `rend_notice.py` v2 (pas à pas, phrase suivante affichée, `--no-open` en recette) ; `notice.py` complété ; `notice/LISEZ-MOI.html` ; `README.md` (trois gestes) ; `fabrique.py` sur `skills/cortex-*` par glob ; `PROVENANCE.md` : verdict des annexes internes qui portent des marques (traiter ou retirer, sur décision du chef d'orchestre) | `skills/cortex-0-poste/`, `outils/`, `notice/`, `skills/cortex-4-installation/scripts/{etat,rend_notice,notice}.py`, `skills/cortex-4-installation/scripts/notice.md`, `fabricant/`, `README.md`, `PROVENANCE.md` | `python3 skills/cortex-0-poste/scripts/poste.py --dry-run` imprime une ligne par outil manquant avec la commande de l'OS courant, nombre de lignes = outils du kit absents ; `grep -cE "https?://" notice/LISEZ-MOI.html` = 0 ; après un maillon 0 sur un atelier vide, `etat.json` porte `notice_ouverte_le` ; `python3 fabricant/scripts/fabrique.py --sortie /tmp/z.zip` sort 0 et le zip compte 8 maillons présents + 6 annexes | à lancer |
| **C Profils et doctrine** (après A2) | `cortex-1` : question profil (langage ordinaire), `references/profils/{employe,dirigeant,societe}.md` (questions, substrats attendus, plafonds, domaines de départ, pièges), régime de donnée fixé (`04-contrat.md` §2), bloc `collecte.racines` ; `cortex-3` : entretien de compréhension (`04-contrat.md` §8) ; `cortex_config.py` valide `profil` et `donnees.regime` dans `valider_installable` ; `secteurs.md` : dénominateur des seuils = notes | `skills/cortex-1-cadrage/`, `skills/cortex-3-ontologie/`, `skills/cortex-4-installation/scripts/cortex_config.py`, `skills/cortex-4-installation/template/config.example.yaml` | `cortex_config.py` refuse `profil: autre` et `regime: mixte` avec un message qui nomme la clé ; trois cadrages rejoués sur `recette/fixtures/<profil>/` produisent trois `config.yaml` que `valider_installable` accepte ; sur la fixture employé, les cinq écarts d'`ECARTS.json` produisent cinq blocs `[?]` dans `02-ontologie.md` | à lancer |
| **D Scan outillé** (après A1 ; lit `collecte.racines` du contrat) | `cortex-2` : `scripts/scan.py` (stdlib, disque seul, bornes mesurées en entiers, dépôts git, bases détectées, extensions, dates) ; extraction de texte par `uvx markitdown` bornée en octets pour les candidats structurants ; bloc `mail` rempli par l'agent selon `04-contrat.md` §7 ; Graphify proposé sur détection d'un dépôt de code ou d'un dossier de plus de 500 documents | `skills/cortex-2-inventaire/` | `python3 skills/cortex-2-inventaire/scripts/scan.py --racine skills/cortex-4-installation/recette/fixtures/dirigeant --config <config> --out /tmp/inv.json` produit un JSON conforme au schéma §4 du contrat, sans champ `contenu`, `bornes` en entiers ; `time` inférieur à 60 s ; sur la fixture employé, `export-notion-*.csv` apparaît comme signal de base déportée | à lancer |
| **E Couche vault** (après A2) | `scaffold.py` : `settings.json` avec allow lecture, `additionalDirectories` et `deny` Write/Edit depuis `collecte.racines`, hooks SessionStart et Stop, `{{DOSSIERS_PROJETS}}` substitué en forme `~` (règle le défaut n°7 de la v1 sans toucher le lint), `gh repo create --private` proposé ; skills vault `parle`, `bilan`, `cloture` étendue (export `_export/`, régime copie) ; `cortex-5` : copie sélective par lots de quatre jusqu'à `sante.max_structurants`, `structurant_perime` ; `cortex-7` : suivi J+7 et J+30 ; `lint_sante.py` conditionnel au régime | `skills/cortex-4-installation/{SKILL.md,scripts/scaffold.py,scripts/lint_sante.py,template/}`, `skills/cortex-5-ingest/`, `skills/cortex-6-agents-metier/`, `skills/cortex-7-passation/` | dans un vault scaffoldé depuis `config.example.yaml` : `claude -p "compte les fichiers de <racine>" --output-format json` rend `permission_denials` vide ; `claude -p "écris un fichier x dans <racine>/y" --output-format json` rend un refus ; `parle` rend au moins un lien `[[ ]]` que `lint_sante.py` résout ; un structurant modifié à la source lève `structurant_perime` ; `lint_sante.py --vault <vault>` = 0 en régime pointeur et en régime copie | à lancer |
| **F Fédération** (après A2 ; lit le format `_export/` du contrat) | `cortex-8-federation` (SKILL.md), `scripts/federe.py` (stdlib), `federation.yaml`, vault commun généré : Centre, Projets, Acteurs, Domaines fusionnés, chaque note avec `source_vault`, `README.md` « généré, ne pas éditer », empreinte `.cortex-genere` | `skills/cortex-8-federation/` | trois exports fictifs (`recette/fixtures/societe/*`, exports construits selon §6 du contrat) donnent un commun ; `diff -r` de deux générations ne diffère que sur `genere_le` ; une note `visibilite: prive` est absente du commun ; le `README.md` généré est présent | à lancer |
| **G Recette** (préparatoire dès A2, merge après B à F) | `parcours_blanc.py` : neuf étapes, trois profils sur `fixtures.py`, fédération, contrôle des manifestes plugin, grep white-label sur `skills/ notice/ outils/ README.md`, critères C1 à C10 ; rouge jusqu'au merge des lanes, c'est la cible ; `06-verification.md` ; sonde Cowork ; maillon 0 et installation du plugin sur la machine Windows du fabricant | `skills/cortex-4-installation/recette/{parcours_blanc.py,fixtures.py}`, `chantiers/cortex-v2/06-verification.md` | recette à 0 après merge ; tableau C1 à C10, chaque ligne avec commande et sortie ; `grep -rwiE "<liste 01-cadrage>" skills/ notice/ outils/ README.md` vide ; audit à froid signé | à lancer |
| **H Parcours réel** (après G) | `~/Cortex/<slug>/`, profil dirigeant, dossiers synchronisés et cinq boîtes via `mcp-email`, comparaison avec le vault personnel du fabricant | correctifs ciblés | tableau domaines, projets, acteurs retrouvés et manqués, taux de rappel chiffré, verdict du fabricant tracé par une ligne datée | après G |
| **I Remise** | tag `v2.0.0`, zip, README final, fiche de chantier et note de décision dans le vault du fabricant, redirection du dépôt d'origine, cases ouvertes fermées | dépôt, vault du fabricant | recette à 0 sur le tag ; `claude plugin marketplace add cortex-kit/cortex && claude plugin install cortex@cortex-kit` depuis GitHub | après H |

## Points hors lane, tenus par le chef d'orchestre

- Création de l'org GitHub `cortex-kit` et du dépôt public, fork de `mcp-email` sous `cortex-kit`.
- Sonde Cowork (une commande `uvx markitdown --version` dans le bac à sable) avant la Phase G.
- Décision sur les quatre annexes internes qui portent des marques : traiter ou retirer du kit.
- Nom de la machine Windows pour la Phase G.
